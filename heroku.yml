build:
  docker:
    web: Dockerfile
release:
  command:
    - python3 manage.py migrate
  image: web
run:
  web: uwsgi --master /etc/uwsgi/whiteboard.ini && python3 manage.py collectstatic --noinput
